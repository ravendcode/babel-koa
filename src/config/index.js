import {
  env,
  port,
  rootDir,
  templatesDir,
  staticDir,
  publicDir,
  nodeModulesDir,
} from './base';
import routes from './routes';

export default {
  env,
  port,
  rootDir,
  templatesDir,
  staticDir,
  publicDir,
  nodeModulesDir,
  routes,
};
