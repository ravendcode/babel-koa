import {
  env,
  port,
  resourcesDir,
  templatesDir,
  staticDir,
  publicDir,
  nodeModulesDir,
} from './base';
import routes from './routes';

export default {
  env,
  port,
  resourcesDir,
  templatesDir,
  staticDir,
  publicDir,
  nodeModulesDir,
  routes,
};
